name: Test My Portfoilo App

on:
  push:
    branches: 
      - main
      - dev

  pull_request:
    branches: 
      - main    

jobs:
  test:
   runs-on: ubuntu-latest

   steps:
     - uses: actions/checkout@v4

     - uses: actions/setup-node@v4
       with:
        node-version: 24.11.1

     - run: npm install
     - run: npm run build
     - run: echo "Tests completed successfully!"
